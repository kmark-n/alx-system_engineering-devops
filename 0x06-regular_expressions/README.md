A regular expression commonly called "regexp", is a sequence of characters that define a search pattern. It operates like a find and replace pattern.
Example /scho*l/ -> schoool
	/scho.l/ -> scho.l and school
